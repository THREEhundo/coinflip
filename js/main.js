//Make an api request using async await

// get the response

// if response is heads --> set image src to heads
// if response is tails --> set image src to tails

async function randomNum() {
	const beep = await fetch()
}
